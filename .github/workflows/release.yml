name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  spk:
    needs: goreleaser
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # x86_64 — all use the same linux/amd64 binary
          - { syno_arch: apollolake,  goarch: amd64 }
          - { syno_arch: geminilake,  goarch: amd64 }
          - { syno_arch: r1000,       goarch: amd64 }
          - { syno_arch: v1000,       goarch: amd64 }
          - { syno_arch: denverton,   goarch: amd64 }
          - { syno_arch: broadwell,   goarch: amd64 }
          - { syno_arch: broadwellnk, goarch: amd64 }
          # arm64 — linux/arm64 binary; covers newer ARM NAS models
          - { syno_arch: armada37xx,  goarch: arm64 }
          - { syno_arch: rtd1296,     goarch: arm64 }
          - { syno_arch: rtd1619b,    goarch: arm64 }
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Derive version
        id: ver
        run: |
          TAG="${GITHUB_REF_NAME}"
          SEMVER="${TAG#v}"
          echo "semver=${SEMVER}" >> "$GITHUB_OUTPUT"
          echo "spk_version=${SEMVER}-0001" >> "$GITHUB_OUTPUT"

      - name: Build binary
        run: |
          GOOS=linux GOARCH=${{ matrix.goarch }} go build \
            -ldflags "-s -w -X github.com/jmsnll/cardimportd/internal/version.Version=${{ steps.ver.outputs.semver }}" \
            -o cardimportd-linux-${{ matrix.goarch }} \
            ./cmd/cardimportd

      - name: Build SPK
        run: |
          make _spk \
            ARCH=${{ matrix.syno_arch }} \
            BINARY_SUFFIX=linux-${{ matrix.goarch }} \
            SPK_VERSION=${{ steps.ver.outputs.spk_version }}

      - name: Upload SPK to release
        run: |
          gh release upload "${{ github.ref_name }}" \
            "cardimportd-${{ steps.ver.outputs.spk_version }}-${{ matrix.syno_arch }}.spk" \
            --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  pages:
    needs: spk
    runs-on: ubuntu-latest
    steps:
      - name: Download SPK files
        run: |
          TAG="${GITHUB_REF_NAME}"
          SEMVER="${TAG#v}"
          SPK_VER="${SEMVER}-0001"
          mkdir -p /tmp/pages
          for ARCH in apollolake geminilake r1000 v1000 denverton broadwell broadwellnk \
                      armada37xx rtd1296 rtd1619b; do
            curl -fsSL -o "/tmp/pages/cardimportd-${SPK_VER}-${ARCH}.spk" \
              "https://github.com/jmsnll/cardimportd/releases/download/${TAG}/cardimportd-${SPK_VER}-${ARCH}.spk"
          done

      - name: Generate packages.json
        run: |
          TAG="${GITHUB_REF_NAME}"
          SEMVER="${TAG#v}"
          SPK_VER="${SEMVER}-0001"
          python3 -c "
import json, sys
base, spk_ver = sys.argv[1], sys.argv[2]
archs = sys.argv[3:]
packages = [{
    'package': 'cardimportd',
    'version': spk_ver,
    'dname': 'Card Importer',
    'desc': 'Watches for SD/CFExpress card insertions and imports photos/videos.',
    'arch': [arch],
    'distributor': 'jmsnll',
    'link': f'{base}/cardimportd-{spk_ver}-{arch}.spk',
    'qinst': True,
    'qupgrade': True,
    'qstart': True,
} for arch in archs]
with open('/tmp/pages/packages.json', 'w') as f:
    json.dump({'packages': packages}, f, indent=2)
    f.write('\n')
" \
            "https://jmsnll.github.io/cardimportd" \
            "${SPK_VER}" \
            apollolake geminilake r1000 v1000 denverton broadwell broadwellnk \
            armada37xx rtd1296 rtd1619b

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: /tmp/pages
          publish_branch: gh-pages
          keep_files: true
          commit_message: "Update packages feed for ${{ github.ref_name }}"
